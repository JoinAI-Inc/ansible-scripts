# 要清理的目录（必须是绝对路径）
cleanup_dirs:
    - /root/ComfyUI/input
    - /root/ComfyUI/output

# 清理阈值：分钟（更稳定）
cleanup_age_minutes: 60

# 是否删除空目录（仅删除 empty 目录）
cleanup_remove_empty_dirs: true

# 安全保护：禁止清理根目录
cleanup_deny_root: true
